# ── OpenAI ───────────────────────────────────────────────────────────────────
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ── Pinecone ─────────────────────────────────────────────────────────────────
# Get your key at: https://app.pinecone.io/ → API Keys
PINECONE_API_KEY=pcsk_...

# Cloud region for your Pinecone serverless index (e.g. us-east-1, eu-west-1)
PINECONE_ENVIRONMENT=us-east-1

# Cloud provider: aws | gcp | azure
PINECONE_CLOUD=aws

# Name of your Pinecone index (will be created automatically if missing)
PINECONE_INDEX_NAME=rag-index

# ── Embedding / LLM settings (optional overrides) ────────────────────────────
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSION=1536
# LLM_MODEL=gpt-4o-mini
# LLM_TEMPERATURE=0.1
# LLM_MAX_TOKENS=1024

# ── Chunking (optional overrides) ────────────────────────────────────────────
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# ── Retrieval (optional overrides) ───────────────────────────────────────────
# DEFAULT_TOP_K=5
# SIMILARITY_THRESHOLD=0.0

# ── Rate limiting (optional overrides) ───────────────────────────────────────
# MAX_CONCURRENT_REQUESTS=5
# EMBEDDING_BATCH_SIZE=100
# UPSERT_BATCH_SIZE=100

# ── Logging ───────────────────────────────────────────────────────────────────
# LOG_LEVEL=INFO
